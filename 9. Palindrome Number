bool isPalindrome(int x)
{
    int org=x;
    long long rev=0;
    if(x<0){
        return false;
    }
    else{
         while(x>0){
            rev=rev*10+x%10;
            x/=10;
         }
    }
     return org == rev;

}
